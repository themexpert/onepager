const React = require('react');

let Sidebar = React.createClass({
  render() {

    return (
      <div class="sidebar">
        Hello Yoyo world
      </div>
    );
  }
});

module.exports = Sidebar;
